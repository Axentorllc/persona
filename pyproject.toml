[project]
name = "persona"
authors = [{ name = "Axentor, LLC", email = "apps@axentor.co" }]
description = "Allows administrator to impersonate another user."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "persona"
